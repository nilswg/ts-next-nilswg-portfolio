const ProjectTag = ({ text }: { text: string }) => (
  <p className="align-center my-1 mr-2 rounded-full bg-gray-200 px-3 py-1 text-[0.5rem] font-semibold text-gray-500">
    {text}
  </p>
)

export default ProjectTag
